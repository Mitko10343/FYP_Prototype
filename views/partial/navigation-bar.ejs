
<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="navbar-header border-right">
            <a class="navbar-brand" href="/">Boom Box</a>
        </div>
        <ul class="navbar-nav mr-auto">
            <li class=" nav-item active"><a class="nav-link" href="/">Home</a></li>
            <li class="nav-item"><a class="nav-link " href="/loginSpotify">Spotify Login</a></li>
        </ul>
        <ul class="navbar-nav">
            <li id="register" class="nav-item"><a class="nav-link" href="/register"><span ></span> Sign Up</a></li>
            <li id="login" class="nav-item"><a class="nav-link" href="/login" data-toggle="modal" data-target="#loginModal"><span ></span> Login</a></li>
        </ul>
        <ul class="navbar-nav" >
            <li id="profile" class="nav-item"><a class="nav-link" href="/profile">Profile</a></li>
            <li id="signout" class="nav-item"><a class="nav-link" id="signOut" href="#"> Sign out</a></li>
        </ul>
</nav>

<div class="modal fade" id="loginModal">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">

            <!-- Modal Header -->
            <div class="modal-header">
                <h4 class="modal-title">Login</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>

            <!-- Modal body -->
            <div class="modal-body">
                <form class="form-horizontal" id="loginForm" method="post" action="/login">
                    <div class="form-group">
                        <label for="email">
                            E-mail:
                            <input type="text" name="email" class="form-control"/>
                        </label>
                    </div>
                    <div class="form-group">
                        <label for="pwd">
                            Password:
                            <input type="password" name="pwd" class="form-control"/>
                        </label>
                    </div>
                    <div class="form-group">
                        <button type="submit" id="loginBtn" class="btn btn-info">
                            Log In
                        </button>
                    </div>
                </form>
            </div>

            <!-- Modal footer -->
            <div class="modal-footer">
                <a  class="btn btn-success" href="/loginSpotify">Spotify Login</a>
            </div>

        </div>
    </div>
</div>
<script>
    /*// Initialize Firebase
    let config = {
        apiKey: "AIzaSyBYtyCJQ-aTZFYWpvZH_4R8hOnvpWXLuZM",
        authDomain: "prototype-7eefd.firebaseapp.com",
        databaseURL: "https://prototype-7eefd.firebaseio.com",
        projectId: "prototype-7eefd",
        storageBucket: "prototype-7eefd.appspot.com",
        messagingSenderId: "146318673930"
    };
    firebase.initializeApp(config);
    const login = document.getElementById("login");
    const register = document.getElementById("register");
    const signOutButton = document.getElementById("signOut");
    const signout = document.getElementById("signout");
    const profile = document.getElementById("profile");

    signOutButton.addEventListener("click",()=> {
        firebase.auth().signOut().then(()=>{
            console.log("User Signed out");
        }).catch(error =>{
            console.log(`Error : ${error.message}`);
        })
    });


    firebase.auth().onAuthStateChanged((user)=> {
        if (user) {
            login.style.display = "none";
            signout.style.display = "inline";
            profile.style.display = "inline";
            register.style.display = "none";
        } else {
            login.style.display = "inline";
            signout.style.display = "none";
            profile.style.display = "none";
            register.style.display = "inline";
        }
    });*/
</script>

